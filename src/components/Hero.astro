<script>
  import gsap from "gsap";
  const timeline = gsap.timeline();

  timeline
    .from("#heroBadge", {
      opacity: 0,
      y: -20,
      duration: 0.6,
      ease: "power2.out",
    })
    .from(
      "#heroTitle",
      { opacity: 0, y: 20, duration: 0.8, ease: "power2.out" },
      "-=0.4",
    )
    .from(
      "#heroSubtitle",
      { opacity: 0, y: 20, duration: 0.8, ease: "power2.out" },
      "-=0.6",
    )
    .from(
      "#heroCta",
      { opacity: 0, scale: 0.9, duration: 0.8, ease: "power2.out" },
      "-=0.6",
    )
    .from(
      "#heroStats .stat",
      { opacity: 0, y: 20, stagger: 0.2, duration: 0.8, ease: "power2.out" },
      "-=0.6",
    )
    .from(
      "#heroVisual",
      { opacity: 0, scale: 0.95, duration: 1, ease: "power2.out" },
      "-=0.8",
    )
    .from(
      ".hero__float",
      { opacity: 0, y: 30, stagger: 0.3, duration: 0.8, ease: "power2.out" },
      "-=0.6",
    );

  gsap.from("#scrollIndicator", {
    opacity: 0,
    y: 10,
    duration: 1,
    ease: "power2.out",
    delay: 1.5,
  });
</script>

<section class="hero">
  <div class="container hero__inner">
    <div class="hero__content">
      <div class="section-label" id="heroBadge">
        <span class="dot"></span>
        Expertos en Desarrollo Digital
      </div>
      <h1 id="heroTitle">
        Impulsamos tu negocio con <span class="text-gradient">presencia digital</span> de alto impacto
      </h1>
      <p id="heroSubtitle">
        Diseñamos y desarrollamos Landing Pages, Sitios Web y Aplicaciones (Móvil & Desktop) de alto rendimiento, optimizadas para convertir y escalar tu marca.
      </p>
      <div class="hero__actions" id="heroCta">
        <a href="#contacto" class="btn btn-primary">
          Comenzar mi proyecto
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M5 12h14M12 5l7 7-7 7" />
          </svg>
        </a>
        <a href="#proyectos" class="btn btn-outline">Ver portafolio</a>
      </div>
      
      <div class="hero__stats" id="heroStats">
        <div class="stat">
          <span class="stat-num">50+</span>
          <span class="stat-text">Proyectos Exitosos</span>
        </div>
        <div class="stat">
          <span class="stat-num">99%</span>
          <span class="stat-text">Clientes Satisfechos</span>
        </div>
      </div>
    </div>

    <div class="hero__visual" id="heroVisual">
      <div class="visual__main">
        <img src="/images/photo-1498050108023-c5249f4df085.avif" alt="Desarrollo de aplicaciones" />
        <div class="visual__overlay"></div>
      </div>
      
      <!-- Floating Elements -->
      <div class="hero__float float--1">
        <div class="float-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-smartphone"><rect width="14" height="20" x="5" y="2" rx="2" ry="2"/><path d="M12 18h.01"/></svg>
        </div>
        <div class="float-text">App Móvil</div>
      </div>
      <div class="hero__float float--2">
        <div class="float-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-layout"><rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="9" y1="21" x2="9" y2="9"/></svg>
        </div>
        <div class="float-text">Web Apps</div>
      </div>
      <div class="hero__float float--3">
        <div class="float-icon">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-trending-up"><polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/></svg>
        </div>
        <div class="float-text">SEO Pro</div>
      </div>
    </div>
  </div>

  <div class="hero__scroll" id="scrollIndicator">
    <span class="scroll-text">Explorar más</span>
    <div class="scroll-line"></div>
  </div>
</section>

<style>
  .hero {
    padding-top: 10rem;
    padding-bottom: 6rem;
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
    background: var(--gradient-hero);
  }

  .hero::after {
    content: '';
    position: absolute;
    top: -10%;
    right: -10%;
    width: 60%;
    height: 80%;
    background: radial-gradient(circle, rgba(108, 63, 197, 0.15) 0%, transparent 70%);
    pointer-events: none;
    z-index: 1;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 2;
  }

  .hero__content {
    max-width: 640px;
  }

  .text-gradient {
    background: linear-gradient(135deg, #fff 0%, #9b6dff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero__content h1 {
    margin-bottom: 1.5rem;
    letter-spacing: -0.02em;
  }

  .hero__content p {
    font-size: 1.25rem;
    margin-bottom: 2.5rem;
    max-width: 560px;
  }

  .hero__actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    margin-bottom: 4rem;
  }

  .hero__stats {
    display: flex;
    gap: 3rem;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .stat-num {
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--white);
  }

  .stat-text {
    font-size: 0.85rem;
    color: var(--white-dim);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* Visual Section */
  .hero__visual {
    position: relative;
  }

  .visual__main {
    border-radius: var(--radius);
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 40px 100px rgba(0, 0, 0, 0.5);
    aspect-ratio: 4/3;
  }

  .visual__main img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .visual__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, var(--blue-dark) 0%, transparent 40%);
  }

  /* Floating elements */
  .hero__float {
    position: absolute;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 0.75rem 1rem;
    border-radius: 100px;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    z-index: 3;
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-15px); }
  }

  .float--1 { top: 10%; left: -10%; animation-delay: 0s; }
  .float--2 { top: 60%; right: -5%; animation-delay: 2s; }
  .float--3 { bottom: 5%; left: 10%; animation-delay: 4s; }

  .float-icon { font-size: 1.25rem; }
  .float-text { font-weight: 600; font-size: 0.85rem; }

  /* Scroll indicator */
  .hero__scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .scroll-text {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--white-dim);
  }

  .scroll-line {
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, var(--purple-light), transparent);
  }

  @media (max-width: 1024px) {
    .hero__inner {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 5rem;
    }
    .hero__content { margin: 0 auto; }
    .hero__actions { justify-content: center; }
    .hero__stats { justify-content: center; }
    .hero__visual { max-width: 600px; margin: 0 auto; }
  }

  @media (max-width: 640px) {
    .hero__actions { flex-direction: column; width: 100%; }
    .hero__actions .btn { width: 100%; justify-content: center; }
    .float--1 { left: 0; }
    .float--2 { right: 0; }
  }
</style>
